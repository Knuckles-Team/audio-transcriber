{
  "mcpServers": {
    "audio": {
      "command": "audio-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "AUDIO_URL": "${AUDIO_URL:-http://localhost:8080}",
        "AUDIO_TOKEN": "${AUDIO_TOKEN}"
      }
    }
  }
}
